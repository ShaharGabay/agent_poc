FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY travel_service.py .

EXPOSE 8002

CMD ["fastmcp", "run", "travel_service.py", "--transport", "http", "--port", "8002", "--host", "0.0.0.0"]
